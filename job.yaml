apiVersion: batch/v1
kind: CronJob
metadata:
  name: hellocron
spec:
  schedule: "*/3 * * * *" #Runs every 3 minutes
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: api
            image: 211104/api:v-5b2c929
            command: ['php', 'artisan', 'migrate']
          restartPolicy: OnFailure
      backoffLimit: 6
  suspend: false